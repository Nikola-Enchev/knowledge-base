
formGroup = this.fb.group({})
replacementUnitPriceFormGroup = this.fb.group({})

// Unclear naming
const sendData = () => {
  this.uploadMaterialsEvent.emit(this.materialForm);
}

// Inconsistent naming
fetchExcludedMaterialsData() 

getSideNavData() 

// Other:
setSidebarShowing() 